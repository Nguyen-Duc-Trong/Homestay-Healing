import db from '../models'

// GET ALL CATEGORY
export const posting  = () => new Promise(async (resolve, reject) => {
    try {
        let a = "aaaaaa"
        resolve(a)
    } catch (error) {
        reject(error)
    }
})